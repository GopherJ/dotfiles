[file-run]
command="$(VIM_FILEPATH)"
command:c,cpp="$(VIM_PATHNOEXT)"
command:rust="$(VIM_PATHNOEXT)"
command:go="$(VIM_PATHNOEXT)"
command:python=python "$(VIM_FILENAME)"
command:javascript=node "$(VIM_FILENAME)"
command:sh=sh "$(VIM_FILENAME)"
command:kotlin=java -jar "$(VIM_PATHNOEXT).jar"
output=terminal
cwd=$(VIM_FILEDIR)
save=2

[gen-tags]
command=ctags -R -f tags --exclude=.git --exclude=node_modules --exclude=target .
cwd=$(VIM_ROOT)
errorformat=

[file-build]
command:c,cpp=gcc -O2 -Wall "$(VIM_FILEPATH)" -o "$(VIM_PATHNOEXT)" -lstdc++ -lm -msse3
command:rust=rustc "$(VIM_FILEPATH)"
command:go=go build -o "$(VIM_PATHNOEXT)" "$(VIM_FILEPATH)"
command:kotlin=kotlinc "$(VIM_FILEPATH)" -include-runtime -d "$(VIM_PATHNOEXT).jar"
output=quickfix
cwd=$(VIM_FILEDIR)
save=2

[project-run]
command=make run
cwd=$(VIM_ROOT)
output=terminal

[project-build]
command=make
cwd=$(VIM_ROOT)
